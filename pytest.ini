[pytest]
testpaths = ./tests
pythonpath = ./src

markers =
    integration: tests that require optional heavy deps (sentence-transformers, numpy, etc.)
    synthesis: tests that require the synthesis optional extras (openai, httpx, pillow)
